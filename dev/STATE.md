# STATE — Authoria Requiem Patching Skills (cross-session ledger)

*Class: ARCHIVE (per `standards/HOUSECARL_DOC_HYGIENE.md`) — the build-era cross-session ledger, frozen at ship (2026-06-09). SUPERSEDED as boot-read authority: sessions now read the latest `dev/session-handoffs/` handoff first (the line below is the historical build-arc rule, kept as written).*

*This is the single source of truth across sessions. A master/orchestrator session runs Phase 0 and Phase 9; each domain phase (1–8) runs in its own fresh session driven by a handoff prompt. **Every session reads this file first.***

## Mission

Author a fresh **houseCARL skill plugin** (`authoria-requiem`) so anyone can say *"patch mod X for Requiem"* and get a correct, Requiem-consistent **direct ESP override** (records + Papyrus scripts where needed) for new weapons, armor, ammo, NPCs, races, leveled lists/containers, spells, or any new record. The patch is later run through the Reqtificator, which applies the auto-balance pass.

Governing spec: **`C:\Users\Heisen\Downloads\HOUSECARL_SKILL_AUTHORING.md`** (Aaron's locked standard). This is a clean build — do **not** reference the old `authoria-requiem-skills` archive.

## Start-of-session checklist (every phase)

1. Read this `STATE.md`, `docs/scope-and-authority.md`, `docs/reqtificator-rules.md`, `docs/masters-and-null-stripping.md`.
2. Read `HOUSECARL_SKILL_AUTHORING.md` (the standard you author to).
3. Invoke `anthropic-skills:skill-creator` via the Skill tool (standard §1 — mandatory at authoring time).
4. Confirm houseCARL is fresh: read Iron Sword `012EB7:Skyrim.esm` (`conflict_tree=true`) → winner must be `Requiem.esp`. If it's `Requiem for the Indifferent.esp` or an `Authoria - *` plugin, re-point: `housecarl_set_mo2_instance path="D:\Wabbajack\Authoria-dev"`, then re-check.
5. Do the phase. Update the ledger below + append notes. (Commit only if asked.)

## Locked decisions

- **Authority** = houseCARL live conflict winner among the **~2841 active** plugins. The disabled Authoria overlay (`Requiem for the Indifferent.esp`, `Authoria - Reqtificated/CC … Reqtificated`, Synthesis/CK/xEdit/RFTI/NPC-Merge outputs) is auto-excluded. Details in `docs/scope-and-authority.md`.
- **Output** = direct `.esp` overrides via houseCARL `create_record`/`set_field`/`bulk_apply` (so the Reqtificator sees them). SkyPatcher/SPID/KID are not the default form. Papyrus scripts in scope; the Reqtificator config is **not** extended.
- **Depth** = hybrid: derive rules by sampling + reading the live config; exhaustive catalogs only for small bounded sets (races, materials, ammo, keyword/tempering sets).
- **Method** = live-analogy (see below). Never hardcode numbers derivable from a live comparable.

## Per-skill construction method (identical every domain)

1. **Mine live** — `cross_plugin_query` by record type scoped to the in-scope Requiem plugins; `read_record`/`batch_record_detail` + `conflict_tree` to get authoritative stats/keywords/perks and the delta vs. vanilla. Sample representatives; exhaustive only for bounded sets.
2. **Read the live Reqtificator config** for the domain (the underivable rules — caps, keyword maps, perk gates).
3. **Distill** into the skill: a **live-analogy procedure** (*identify the new content → query Requiem's own comparables via houseCARL → derive target stats/keywords/recipe/placement → apply judgment for uniques/novel mechanics → emit the ESP override via houseCARL*), a **small reference layer** of underivable rules (+ `references/index.jsonl`), and **2–3 worked examples** from real in-scope records.
4. **Validate** — eval set (≥8 trigger + ≥8 near-miss; manual-prediction fallback per §6.5, recall ≥80% / specificity ≥50%), archived under the skill's `evals/`; **round-trip** a recipe by reproducing a real in-scope Requiem record from scratch and diffing against the houseCARL winner.

## Skill conventions (condensed from the standard — full rules in §2–§8)

- Folder = `kebab-case` ≤30 chars; `SKILL.md` frontmatter has `name:` **equal to the folder name** + a single-line `description:`.
- **Description (§3):** action-first lead ("Patch … for Requiem via houseCARL — …"); ≥3 user-utterance trigger nouns in a "Use when…"; pushy reinforcement tail ("Load this before …, not after …"); none of the §3.4 forbidden items (version/phase markers, vague qualifiers like "complex", jargon, negative-gated triggers); combined ≤1,536 chars; target 60–180 words.
- **Body (§4):** H1 = folder name in Title Case; sections in order — `## Overview` → `## First step` → workflow/decision → `## Common mistakes` → `## Notes` (omit any that don't apply, never reorder); ≤500 lines (soft <250); imperative + reasoning, not all-caps MUST/NEVER; show **real houseCARL call shapes**, not pseudocode; no embedded changelog/TODO/verbatim-tool-schema/exhaustive-enumerations (lift big tables to `references/`).
- **References:** `references/<topic>.md` + a grep-friendly `references/index.jsonl` (topic → file + line).
- **Evals:** archived under `evals/eval_set.json`.
- **§8 19-item checklist:** audited per skill (Phase 9 does the final audit).

## Plugin layout (scaffolded ✅)

```
.claude-plugin/marketplace.json          (authoria-requiem-local)
authoria-requiem/.claude-plugin/plugin.json
authoria-requiem/skills/<skill>/{SKILL.md, references/, evals/}
docs/{scope-and-authority.md, reqtificator-rules.md}
STATE.md
```

## Phase ledger

| Phase | Skill | Status | Notes |
|---|---|---|---|
| 0 | scaffold + scope lock + docs base | ✅ done | houseCARL re-pointed to Authoria-dev; authority verified (Iron Sword → Requiem.esp); scaffold + docs written |
| 1 | `requiem-weapon-patching` | ✅ done | pilot template proven; round-trip exact (Orcish War Axe). **Scope grew: staves folded in → MR now in weapon scope (see notes + Phase 7 impact).** Ammo stays separate (Phase 3). |
| 2 | `requiem-armor-patching` | ✅ done | AR ladder + per-part ratio; set-keyword drives resist+tempering (Reqtificator-assigned); heavy-gauntlet fist perk; shields; round-trip exact ×2 |
| 3 | `requiem-ammo-patching` | ✅ done | exhaustive arrow+bolt ladder; **all ammo keywords source-carried** (no Reqtificator ammo assignment); AP-tier + AmmoWeight (bow-tier pairing); PROJ profile; round-trip exact ×2. Stays standalone, cross-refs weapons |
| 4 | `requiem-race-patching` | ✅ done | playable + creature races; two-layer trait model (ActorEffect spells inherited vs per-NPC perk); **Authoria Master Patch is race authority** (scope exception); round-trips ×3. NPC per-actor perk bridge → Phase 5 |
| 5 | `requiem-npc-patching` | ✅ done | visual-vs-balance split; fixed level + remove PCLevelMult (followers keep it); REQ class/combat-style taxonomy; source-carried combat perks vs Reqtificator-added trait/gameMechanics perks; template chain; 4 patch-classes incl. **bosses**; trait bridge reused from Phase 4; follower registration = **script-side (Phase 8)**; round-trips ×3 |
| 6 | `requiem-leveled-list-patching` | ✅ done | additive merge (add-only, master Requiem per-plugin, ≥3 to fire); Level-1 + repetition tiering; container = themed list refs; ECZN = DisableCombatBoundary only (not de-level); REQ_NULL-in-lists nuance; round-trip exact + 2 live writes |
| 7 | `requiem-magic-patching` | ✅ done | MGEF/SPEL/ENCH design; explicit cost (ManualCostCalc); HalfCostPerk = school+tier classifier; multi-effect shape; tomes/scrolls/staves/projectile/alchemy; three-layer split (record / RFTI_All_* rescaling / Nox_* script); round-trip exact (Ice Spike from Firebolt) + live write |
| 8 | `requiem-script-patching` + `requiem-patching` (capstone) | ✅ done | both skills authored; script-vs-no-script gate + Nox runtime map + follower registration; router routes 8 domains + 11 gap mechanics (vampire/lycanthropy kept as a reference, NOT split); integration dry-run on Lucien.esp PASS; §8 1–15+19 pass, 16–18 manual fallback |
| 9 | finalize (consistency, package, §8 audit) | ✅ done | masters/REQ_NULL gate back-filled into all skills; §8 hard items pass on all 9; router verified; packaged v1.0.0; VMAD write round-trip PASS. **PROJECT COMPLETE.** |

### Phase notes (append per phase)

- **Phase 0** (master session, 2026-06-07): houseCARL pointed at `D:\Wabbajack\Authoria-dev`, 2841 active. Authority rule verified. Scaffold + `docs/` + `STATE.md` written. Decided 9 skills (`requiem-*` names) + router. Open question for Phase 1: keep ammo separate (Phase 3) vs. fold into weapons — default keep separate. Phase 1 handoff prompt emitted.

- **Phase 1** (2026-06-07): authored `requiem-weapon-patching` (SKILL.md ~210 lines + 7 references + eval set + round-trip results). Freshness re-verified (Iron Sword → Requiem.esp). Built on live mining of the WAR/MR/SR/ISC + Requiem.esp winners.

  **Template established (Phases 2–8 copy this shape):** `SKILL.md` (action-first description, freshness probe as First step, live-analogy Workflow with real houseCARL call shapes, Judgment, Common mistakes, Checklist, Notes) + `references/` (`<topic>.md` ×N + `housecarl-recipes.md` with copy-ready `create_record`/`set_field`/`bulk_apply` shapes + `index.jsonl`) + `evals/eval_set.json` (§6.5 manual fallback, `manual_predicted_trigger` per query) + `evals/results-<date>.json` (round-trip). §8 audit: hard items 1–15 + 19 pass; 16–18 on the manual fallback (Windows blocks `run_loop`), flagged for empirical re-validation + peer prediction check.

  **Key derived rules (live, verify per record):**
  - **Damage-type keyword (slash AD3954 / ranged AD3955 / pierce AD3956 / blunt AD3957, all `Requiem.esp`) is a Reqtificator OUTPUT** — absent from source records. Patches carry the **weapon-type** keyword and let the Reqtificator assign the damage type (`feature_DamageType` keys off it). Do NOT hand-stamp it.
  - Melee keyword set = `[WeapMaterial<X>, WeapType<Y>, VendorItemWeapon 08F958]`. Bows add `REQ_BowBreakable`, `REQ_WeaponType_Bow{Light/Heavy}`, `RFTI_Exclusions_No{Damage,BowSpeed}Rescale` (these ARE in source) + `Data.Flags=NPCsUseAmmo`. Staves = `[WeapTypeStaff, VendorItemStaff, Nox_KW_Staff_<School><Tier>]` (MR, enchantment-driven), Damage 1.
  - **Crit = floor(Damage/2)** everywhere; PercentMult 1. Speed/Reach/Anim are type-defined.
  - **Damage ladder (1H sword):** Iron 7 → Steel/Silver 8 → Elven 9 → Dwarven 10 → Orcish/Skyforge 11 → Glass 12 → Ebony/Stalhrim 13 → Dragonbone 14 → Daedric 15. **Value ladder (sword baseline):** 25/45/90/135/180/225/315/1125/1800/1800/3600/4500; value = material-base × weapon size-class. **Prefer copying the same-type same-material comparable's value;** interpolate only when none exists.
  - **Crafting:** 3 COBJ per weapon — Forge (`WorkbenchKeyword 088105`), Temper/grindstone (`088108`), Smelter (`0A5CCE`). Perk gate = a `HasPerk` condition whose perk renders as an unreadable form-index → **clone the comparable's `Conditions` verbatim** rather than retyping.
  - **Enchant charge:** weapon `EnchantmentAmount` = 500 × tier (Fire1 500 / Fire2 1000 / Fire3 1500); per-cast cost on the ENCH; enchanted value = base value (no gold bump). Enchanted variant uses `Template` → plain base.
  - **Leveled variants are NULL** (`REQ_Var_*` / leveled stubs) — derive from the canonical strongest record only.
  - **Artifacts deviate** (Dawnbreaker: Value 80,000 bespoke, no charge pool, material keyword repurposed for undead damage, tempering carried as `REQ_Tempering_<Perk>` keyword) — keep the structure, let value/enchant be bespoke.

  **Round-trip (exact):** blind-derived Orcish War Axe from comparables → matched the live winner on all 9 fields (Dmg 12, Crit 6, Val 275, Wt 15, Spd 0.9, Reach 1.0, anim, keywords). Value method (steel→orcish ratio ×5 on the steel war axe's 55 → 275) was exact.

  **Plugins/authority (weapons):** winners come from WAR (`Requiem - Weapons and Armor Redone.esp`, 531 WEAP) > MR (`Requiem - Magic Redone.esp`, 1250 WEAP — staves/bound/artifacts) > SR (`Requiem - Stealth Redone.esp`, 21 WEAP — trap weapons) > `Requiem.esp` (4340 WEAP) + cross-patches. **Sound-bearing winner** for battleaxe/warhammer = `Requiem - Immersive Sounds Compendium.esp` (the Requiem ISC patch; base is `Immersive Sounds - Compendium.esp`); other weapons just reuse the comparable's sound/impact FormID links (ISC overrides audio at SNDR level). Requiem inherits some USSEP misc data — reading the winner folds it in.

  **DECISION for the master / Phase 7:** Aaron folded **staves into Phase 1**, so `requiem-weapon-patching` now covers the staff *frame* (keywords, value, charge, ObjectEffect link). **Trim Phase 7 (`requiem-magic-patching`) to non-weapon magic** — spells/effects/perks, enchantments, and staff *spell/effect design* (MGEF/SPEL/ENCH) — and re-issue its handoff. Ammo confirmed separate (Phase 3).

  **Reuse for later phases:** the reference layout, the `housecarl-recipes.md` call-shape patterns, the §6.5 eval shape, and the round-trip method are the template. Item 19 (build-script `$Skills` registry) is N/A in this repo — skills auto-discover by directory; revisit if Phase 9 packaging adds a build script.

- **Phase 1 field test** (2026-06-08): exercised `requiem-weapon-patching` on three real mods, all patched successfully into `houseCARL - Requiem weapon patching` (Moonblade + Dreadbinder in `Requiem weapon patching.esp`; Dawnguard Arsenal in `Dawnguard Arsenal - Requiem.esp`).
  - **Moonblade** (unique 1H+2H, ench+plain): crit→floor(dmg/2), ebony-tier value, strict speed normalization; **not an artifact** (acquisition trace: craftable + not in loot).
  - **Dreadbinder** (conjuration staff, Mass Reanimate): added `Nox_KW_Staff_Conjuration5` + Damage 1; identified as artifact, author value/charge preserved.
  - **Dawnguard Arsenal** (12 silver weapons): added `WeapMaterialSilver`, removed `DLC1DawnguardItemScript`, fixed crit, applied Requiem's `REQ_Ench_Weapon_TurnUndead4` (+2000 charge) to the 11 plain ones (Elite Hammer kept `REQ_Ench_AncientDawnguardWarhammer`).
  - **Skill improved:** added the **artifact-identification-by-acquisition-trace** ladder (reverse-reference COBJ/LeveledItem; record heuristics like `MagicDisallowEnchanting` only nominate) to `SKILL.md` §Judgment + a Moonblade negative example in `worked-examples.md`. Captured Aaron's cross-phase rules in memory (`patching-preserve-author-intent`).
  - **houseCARL bugs found** (bug report drafted for Aaron, workarounds in memory `housecarl-patch-rename-gotcha`): (1) **active-patch write self-lock** — can't write to a patch that's checked in plugins.txt (Mutagen self-maps it); workaround = author into a fresh inactive patch, move into place. (2) `Remove`-by-index throws on an already-empty list. (3) renaming a patch's mod folder desyncs `meta.ini` + the cached registry.
  - **Open:** `Requiem.esp` as a *direct* master isn't auto-addable when no edit references a Requiem-defined form (e.g. silver weapons using vanilla `WeapMaterialSilver`); needs a manual master add or a Requiem-defined reference. Flag for the master session whether to standardize a mechanism.

- **Phase 2** (2026-06-08): authored `requiem-armor-patching` (SKILL.md 271 lines + 5 references + eval set + 2 round-trips). Freshness re-verified (Iron Sword → Requiem.esp). Built on live mining of WAR + base Requiem.esp ARMO winners + the live `ArmorKeywordAssignments_Requiem.esp.conf` and `Reqtificator.conf`.

  **Template held:** same shape as Phase 1 (action-first description, freshness First step, live-analogy Workflow with real houseCARL call shapes, Judgment, Common mistakes, Checklist, Notes; `references/` = ar-ladder + keywords + crafting + housecarl-recipes + worked-examples + index.jsonl; `evals/` = eval_set + results). §8: hard items 1–15 + 19 pass; 16–18 on the §6.5 manual fallback (Windows blocks `run_loop`), flagged for empirical re-validation + peer check. Item 19 build-script registry N/A (auto-discover).

  **Key derived rules (live, verify per record):**
  - **Naming:** Requiem renames vanilla armor to `REQ_<Weight>_<Material>_<Part>` — parts `Body`(cuirass)/`Head`/`Hands`/`Feet`/`Shield`(+`Buckler`). `REQ_NULL_*`/`REQ_Var_*` are NULLed stubs (skip). WAR owns shields/bucklers; base `Requiem.esp` owns body pieces.
  - **AR scale:** Requiem stores `ArmorRating` at **~10× vanilla** (iron cuirass 250 vs vanilla 25). The `armorRatingThresholds` caps (heavy body 74/feet 27/hands 27/head 35/shield 54; light 62/18/18/26/44) are in the **÷10 scale** → ×10 to compare to the record (heavy body ≤740…). Requiem's top tier (daedric cuirass 600) sits ~80% of cap with headroom; derive from the comparable and you're auto-under-cap. **Don't clamp a record to 74.**
  - **Per-part ratio off the cuirass** (the spine): Body 1.0, Head 0.40, Hands 0.30, Feet 0.30, Shield **AR ×0.60 but value/weight ×0.50**. The same factor scales AR, value, AND weight (shield the one exception). Verified iron/steel/daedric/ebony/elven/glass.
  - **Material spine (cuirass AR / value / weight)** — Heavy: Iron 250/125/20 → Steel 300/250/25 → SteelPlate 400/500/30 → Dwarven 400/750/30 → Orcish 450/1000/30 → Ebony 500/5000/40 → Dragonplate 500/10000/35 → Stalhrim-H 500/5000/40 → Daedric 600/25000/50 (Chitin-H 250/500/20). Light: Fur 125/50/4 → Hide·Leather 150/100/5 → Scale 175/200/7.5 → Elven·Chitin-L 200/400/10 → Dwarven-L 200/500/15 → Glass·Stalhrim-L 275/3000/12.5 → Dragonscale 300/8000/15. **Value tracks the set/material keyword**, jumps hard at glass+.
  - **Source-carried vs Reqtificator-assigned keyword split (the armor analog of the weapon damage-type rule):** source carries `[armorType(heavy 06BBD2/light 06BBD3), armorSet<Material>, armorPart<Part>, VendorItemArmor 08F959]`. **Ranged-resistance tier (`rangedResistance.tier1..5` AD3952…/AD3A25) and tempering perk (`tempering.*` AD3AEC…) are NOT in source — the Reqtificator assigns them** from the set+type keywords (`feature_damageResistances` assigns resist to the **cuirass only**; `feature_tempering` assigns to every piece with the set keyword). **Do not hand-stamp them.** Get the set keyword right and they follow.
  - **Heavy gauntlets carry `PerkFists<Material>`** (Iron 0424EF, Steel 09379E, Ebony 02C178, Daedric 02C17B; others = read off comparable) — the unarmed-damage keyword. **Light gauntlets do NOT.** This is the "fist perk" special rule.
  - **Shields** drop the body-slot part keyword, carry shield part `0965B2`, and carry `BashImpactDataSet` + `AlternateBlockMaterial` (block/bash links — copy from comparable). ISC overrides armor foley at SNDR level (record-level winner for rings/clothing pickup-putdown).
  - **Crafting:** 3 COBJ — Forge (`088105`), **Temper at the armor workbench `0ADB78` (NOT the grindstone `088108` weapons use)**, Smelter (`0A5CCE`). Perk gate = unreadable form-index → **clone the comparable's `Conditions` verbatim**.
  - **Clothing/jewelry frames** (AR-0 ARMO, `ArmorType=Clothing`): `[ArmorClothing 06BBE8/ArmorJewelry 06BBE9, VendorItemClothing 08F95B/VendorItemJewelry 08F95A, slot kw (ClothingBody 0A8657/ClothingRing 10CD09…)]`. No resist/tempering; value tracks material; enchant *effect* → magic phase.
  - **Incidental:** `Survival_ArmorWarm 002ED9` / `Survival_ArmorCold 002ED8` (Update.esm) are climate tags on some sets — copy from comparable, not balance.

  **Round-trip (exact ×2):** blind-derived Ebony Heavy Gauntlets (013962) → AR 150/Val 1500/Wt 12 + PerkFistsEbony, all exact; and Light Dragonscale Gauntlets (01393F) → AR 90/Val 2400/Wt 4.5, no fist perk, exact. Both validate the per-part 0.30 ratio (on all of AR/value/weight) and the heavy-vs-light fist-perk split.

  **Masters/authority:** armor winners from WAR (`Requiem - Weapons and Armor Redone.esp`) > base `Requiem.esp`, + patches (`Requiem - New Legion.esp`, `Requiem - Creation Club.esp`, `Sons of Skyrim - Requiem Patch.esp`). `Requiem.esp` auto-added as master when a Requiem keyword is referenced (resolves the Phase-1 open master item for the armor domain; the general vanilla-only-forms mechanism still deferred to Phase 9).

  **Field test** (2026-06-08): exercised `requiem-armor-patching` on four real mods → `Requiem armor patching.esp` (269 edits / 80 records, all read-back-verified). **New Leather** (6, light) → leather ladder (kept author's correct keywords, normalized AR/val/wt off vanilla scale); **1Fluted** (6, heavy) → steelPlate ladder (author's `PerkFistsSteelPlate` already correct), **pauldron+scarf zeroed**; **Towels** (36) → AR-0 clothing (were AR 10 / Val 500); **DD FVO Cloth** (32) → 30 cosmetic cloth mis-tagged LightArmor+cuirass-keyword converted to clothing frames (AR 0), neck→jewelry, the one real shield→akaviri heavy ladder. Never stamped a Reqtificator keyword. **Skill improved:** added a `## Judgment` subsection "Cosmetic cloth and extra-slot accessories" (mis-tagged cloth → clothing frame; extra-slot pauldron/scarf/cloak → AR 0 since Requiem protection is slot-based) + a Common-mistakes bullet. **Gotcha confirmed:** a brand-new never-enabled patch isn't in houseCARL's registry, so even `read_record plugin="<patch>"` returns "does not touch this record" — the `bulk_apply` per-op read-back is the only verification until enable/refresh (memory `housecarl-patch-rename-gotcha` updated). **Open master item recurs:** none of the four patches auto-mastered `Requiem.esp` (all keywords vanilla/Update-defined incl. the akaviri set 0009C0 from Update.esm) — harmless here (mods' own records, no Requiem clash; Reqtificator keys off keywords not patch masters), but reinforces the Phase-9 standard-master-add candidate.

  **Reuse for later phases:** the source-vs-Reqtificator-assigned keyword split is now confirmed in two domains (weapon damage-type, armor resist+tempering) — a general pattern for all phases (carry inputs, let the Reqtificator assign outputs; never hand-stamp). The per-part ratio + material-spine method generalizes to any tiered item set.

- **Master note** (master session, 2026-06-08): Phase 2 (`requiem-armor-patching`) handoff emitted (`docs/handoffs/phase-2-armor.md`). Resolution of the Phase-1 open item: armor patches naturally reference Requiem-defined ranged-resistance + tempering keywords, so `Requiem.esp` is auto-added as a master; the general "force Requiem.esp master when only vanilla forms are referenced" mechanism is deferred to **Phase 9 packaging** (candidate: a standard houseCARL master-add step in `housecarl-recipes.md`). Staves-folded-into-weapons decision noted for **Phase 7** trim (apply when emitting Phase 7).

- **Master note** (master session, 2026-06-08): Phase 3 (`requiem-ammo-patching`) handoff emitted (`docs/handoffs/phase-3-ammo.md`). Directed an **exhaustive** ammo catalog (bounded domain), the keyword-split check for AMMO, the bow-tier pairing back to Phase 1, and PROJ handling. Ammo confirmed standalone (cross-refs the weapon skill for bows).

- **Phase 3** (2026-06-08): authored `requiem-ammo-patching` (SKILL.md 266 lines + 5 references + eval set + 2 round-trips). Freshness re-verified (Iron Sword → Requiem.esp). Built on **exhaustive** live mining of all ~116 Requiem AMMO winners (WAR owns essentially the whole set over base `Requiem.esp`; bound ammo from the MR/WAR patch). Template held (same shape as Phases 1–2). §8: hard items 1–15 + 19 pass; 16–18 on the §6.5 manual fallback (Windows blocks `run_loop`), flagged for empirical re-validation + peer check. Item 19 build-script registry N/A (auto-discover).

  **Key derived rules (live, verify per record):**
  - **THE ammo keyword split is different — everything is source-carried.** Weapons have the Reqtificator assign the damage-type keyword; armor has it assign resist+tempering. **Ammo has NO keyword-assignment rule in the Reqtificator config at all** — the only ammo line in `Data\*.conf` is `gameMechanics.perks.arrowRecovery = "AD3A35 Requiem.esp"` (a global perk, not a keyword). So the patch **stamps every ammo keyword itself** (material, vendor, AmmoWeight, ArmorPiercing tier, RFTI exclusion). This **breaks the "carry inputs, let the Reqtificator assign outputs" pattern** that held in Phases 1–2 — for ammo there are no assigned outputs. Requiem ammo even carries `RFTI_Exclusions_NoDamageRescale AD3B2D` to keep its hand-tuned damage (the Reqtificator would otherwise rescale).
  - **Damage ladder (arrows, clean +10/tier):** Iron 40 → Steel/Silver 50 → Elven 60 → Dwarven/Quicksilver 70 → Orcish/SkyforgeSteel 80 → Glass 90 → Ebony/Stalhrim 100 → Dragonbone 110 → Daedric 120. **A bolt = the same-material arrow × 1.2, exact** (Steel 50→60, Daedric 120→144). **Weight = 0 for ALL ammo** (heft is the `REQ_AmmoWeight_*` keyword, not the stat — the handoff's "Requiem gives ammo weight" guess is wrong for 6.0.2; weight stays 0, value stays small). Value tracks material, jumping at glass (arrow Iron 1 → Glass 14 → Daedric 55; bolt ≈ ×1.2).
  - **Three INDEPENDENT ladders** — don't infer one from another: (1) **Damage** (+10 / ×1.2). (2) **Armor-piercing tier** `REQ_ArmorPiercing{Arrow,Bolt}_Tier1..7` — tracks penetration rank but **70- and 80-dmg materials both sit at Tier3**, so NOT a function of damage; iron carries none (tier 0); **arrows and bolts use separate keyword series at the same tier number — never cross them**. (3) **AmmoWeight class** `REQ_AmmoWeight_{Light,Medium,Heavy,Massive}` — tracks material **density** not tier (Elven=Light, but Stalhrim only Heavy while lighter-tier Ebony is Massive). **This is the bow-tier pairing** — heavier ammo wants a heavier-draw bow (`REQ_WeaponType_Bow{Light,Heavy}` from Phase 1).
  - **PROJ (projectiles):** Requiem **standardizes physics and overrides even vanilla arrow PROJs** (renames + strips the vanilla `Supersonic` flag from arrows). **Arrows:** Speed 3600 / Gravity 0.35 / `CanBePickedUp` (no Supersonic). **Bolts:** Speed 5600 / Gravity 0.35 / `CanBePickedUp, Supersonic`. PROJ `Type=Arrow` even for bolts. A new arrow can **reuse** a tier-matched Requiem arrow PROJ, or the modded PROJ gets **patched** to this profile (modded arrows commonly fly vanilla-supersonic). We care only about arrow/bolt projectiles — not spell/trap.
  - **Crafting:** one **forge** COBJ (`WorkbenchKeyword 088105`), **1 metal ingot + 1 firewood (`06F993`) → 30 ammo** (the handoff's "24" guess was wrong — it's 30), perk-gate `HasPerk` condition cloned verbatim (unreadable form-index). **No temper/smelter recipe** for ammo (unlike weapons/armor).
  - **Special ammo:** **elemental** (`REQ_Ench_Arrow_<Mat>_Fire/Ice/Shock`) keeps **base-material damage** (Steel Arrow of Fire = 50, not boosted) + modest value bump (1→11) + the **elemental hit lives on the PROJECTILE** (custom PROJ with an `Explosion`), not on AMMO damage — link it, route the explosion/MGEF *design* to the magic phase. **Quest** (Bloodcursed/Sunhallowed): base dmg, bespoke high value (100), special PROJ, not craftable. **Bound:** Value 0, conjured → magic phase. **Creature/siege/trap ammo** (Dwarven Sphere/Ballista, catapult — the "traps" category): carry the **`NonPlayable`** flag, no player recipe. **Flags:** arrow=`NonBolt`, bolt=`0`, creature adds `NonPlayable`.
  - **Silver/Daedric specials:** silver ammo = steel-tier (dmg 50/60) + `WeapMaterialSilver 10AA1A` + `REQ_WeaponType_SilverArrow 0FAB11`; daedric carries `REQ_WeaponType_DaedricArrow AD3A4E` (both arrow + bolt).

  **Round-trip (exact ×2):** (1) blind-derived REQ_Bolt_Orcish from REQ_Arrow_Orcish via the ×1.2 + weight-class-invariance + Arrow→Bolt AP-swap → Dmg 96 / Val 4 / Heavy / Bolt Tier3, all exact. (2) blind-derived REQ_Arrow_Dwarven from REQ_Bolt_Dwarven (÷1.2, Bolt→Arrow swap) → Dmg 70 / Val 3 / Heavy / Arrow Tier3 / NonBolt, exact. Confirms the ×1.2 rule both directions + the AP series swap.

  **Authority (ammo):** winners almost entirely `Requiem - Weapons and Armor Redone.esp` (WAR) over base `Requiem.esp`; bound ammo from `Requiem - MR Weapons and Armor Redone Patch.esp`. `Requiem.esp` auto-masters (ammo references Requiem keywords).

  **Decision — ammo stays STANDALONE** (not merged into weapons): the keyword model (AmmoWeight, dual AP series), the ×1.2 type relationship, the PROJ handling, and the source-carried-everything split are distinct enough to warrant their own skill. Cross-refs `requiem-weapon-patching` for bow/crossbow frames and the bow-tier pairing.

  **Reuse for later phases:** the source-vs-assigned split is now confirmed as a **per-domain question, not a universal law** — Phases 1–2 had assigned outputs, Phase 3 has none. Later phases must **check the live config per domain** rather than assume the Reqtificator assigns something. The "three independent ladders" shape (don't infer one axis from another) generalizes wherever a domain stacks orthogonal keyword systems.

- **Master note** (master session, 2026-06-08): **Reordered per Aaron — races before NPCs** (NPCs inherit race templates/traits). Phase 4 is now `requiem-race-patching`, Phase 5 is `requiem-npc-patching`. Phase 4 (races) handoff emitted (`docs/handoffs/phase-4-races.md`): two sub-domains (playable vs creature), **exhaustive** race catalog, and centrally the **new-race-not-recognized-by-Reqtificator** problem (Reqtificator matches race FormIDs against shipped lists; novel races get nothing → replicate a vanilla race's structure / template onto a known race / hand-assign the creature trait perk per-actor). The per-actor trait-application bridge passes into Phase 5.

- **Phase 4** (2026-06-08): authored `requiem-race-patching` (SKILL.md ~250 lines + 5 references + index.jsonl + eval set + 3 round-trips). Freshness re-verified (Iron Sword → Requiem.esp). Template held (same shape as Phases 1–3). §8: hard items 1–15 + 19 pass (19 N/A — auto-discover); 16+18 pass; 17 on the §6.5 manual fallback (Windows blocks `run_loop`), predicted recall/specificity 1.0, flagged for empirical re-validation + peer check.

  **AUTHORITY CHANGE — races resolve to `Authoria - Master Patch - Races Merge.esp`.** Mid-session Aaron enabled this consolidated race-merge and made it authoritative; it is now the live winner for **49 race records** (all 10 playable + vampire variants + the creatures). It faithfully carries Requiem's trait spells + the knockdown `Attacks` data, **superseding the dated `requiem_knockdown_tweak.esp`** (which drops trait spells when it overrides a race; not in the load order under that exact name — folded into the Master Patch). It also carries Authoria tuning deltas (e.g. TrollRace `Starting[Health]` 300 vs base 150). **This is a deliberate race-domain exception to the Phase-0 scope lock that excluded `Authoria - *` plugins** — justified because it's a corrected/consolidated *input* merge, not the Reqtificated *output* (`Requiem for the Indifferent.esp`). **MASTER ACTION:** record this exception in `docs/scope-and-authority.md`. The freshness probe (Iron Sword → `Requiem.esp`) still validates the weapon/item domains; races legitimately resolve to the Master Patch — do NOT re-point to escape it.

  **Two-layer trait model (the core finding; the source-vs-assigned split for this domain):**
  - **Layer A — trait SPELLS on the RACE's `ActorEffect`** are **engine-inherited by every NPC of the race**, so they propagate to a NEW race for free once carried on the RACE. Families `REQ_Trait_{Resist,Armor,Healing,FX,Cloak,UnarmedDamage}_*` (creature) and `{NoHealthRegeneration,MassEffect,Heritage_*,Blood_*,Physique_*,Cuisine_*,Vampire_*}` (playable). **Almost all `Resist_`/`Armor_`/`Healing_` spells' live winner is `Requiem - Resist and Regen Tweak.esp`.**
  - **Layer B — the `incomingDamageModifier` PERK** is assigned by the **Reqtificator to NPC_ records** by race-FormID match (confirmed via Reqtificator C# `ActorPerksFromRules.cs` AND live: source `EncTroll 023ABA` carries only perk `0FAB0A`, NOT the fur perk `000805` — that's build-time added). **Not inherited from the RACE**, so a new race's NPCs get nothing → the Phase-5 bridge.
  - So the per-domain split is: **carry trait spells + keywords + stats on the RACE; the Reqtificator only adds the per-NPC trait perk.** Two additive Layer-B taxonomies are live: base Requiem per-category (`feature_racialTraits`: skeleton AD3A45, draugr 031285, atronachs AD3A47-49, dragon AD3A62, werewolf AD3A43, …) AND the Resist-and-Regen-Tweak **physique/supernatural** model (`000800`–`00080E`: fur 000805, metal 000807, chitin 000802, stone 00080A, zombie 00080D, supernatural undead 00080B/dragon 000804, …; troll→fur). `doNotInheritTraits AD3A4F` opts out.

  **Practitioner rules baked in (Aaron):** add `REQ_Trait_Armor_*` always; **keep a creature's own `Ab*`-named resistances** (e.g. the Wendigo's `zzzLrhAbBeastPos`), only add armor; `REQ_Trait_Healing_*` requires the **`RegenHpInCombat`** flag (confirmed set on TrollRace); **never add `REQ_Trait_FX_*`** to a modded race; don't duplicate a spit/breath the race already has (patch its own → magic phase).

  **Playable standard — live > Races.md (it drifts a LOT).** Races Redone rebalanced BOTH attributes AND skill boosts; `documentation\Races.md` is base-Requiem intent only (e.g. Redguard doc 130/60/120 +15-OneHanded vs live 100/80/120, OneHanded+10/TwoHanded+10/LightArmor+10). Only the **ability-spell BUNDLE structure** (universal `NoHealthRegeneration 609AF0` + `MassEffect 82CC14` + `Heritage_<Race>` + `Blood_<Race>_*` + `Physique` beast + `Cuisine`) is predictable; read every NUMBER live. Full live table in `references/playable-races.md`. Playable-race perk `RFTI_All_PlayableRace_<Race>` (3E43D8–E4) is per-effect `GetIsRace`-gated → won't transfer to a new FormID; abilities ride on the cloned `ActorEffect` spells instead. ARMA gear: set the new race's `ArmorRace` to the analogue (reuses ARMA coverage) or add it to each ARMA's `AdditionalRaces`.

  **Round-trips ×3 (`evals/results-2026-06-08.json`):** (1) TrollFrost from Troll — trait STRUCTURE exact (Armor shared across troll variants; Resist/Healing per-variant); stats read live. (2) Wendigo Troll (`2E02F7:Glenmoril.esm`, override_depth 1 = unrecognized) classified to troll/fur; confirmed the `Ab*`-keep rule live + fur `000805` for the NPC pass. (3) Redguard — ability bundle predicted exact, attributes AND skill boosts drift from the doc.

  **For Phase 5 (NPC) — the bridge:** per-NPC application of the named Layer-B `incomingDamageModifier` perk is the Phase-4→5 boundary. Two strategies: **template the creature's NPCs onto a known Requiem race** (`set Race`), or **hand-add the named category/physique perk** to the NPCs (`Perks` Add, compose `PerkPlacement`). The live creature-trait taxonomy to use is the **Resist-and-Regen-Tweak's**, not base Requiem's. Recipe shape in `references/housecarl-recipes.md` §G.

  **Reuse for later phases:** the source-vs-assigned split now has a third shape — **engine-inherited (spells on the parent record) vs Reqtificator-assigned (perks on the child)**; check the propagation mechanism, not just the config. The live>doc lesson is sharper here than anywhere (the doc drifts on attributes AND skills) — never trust shipped documentation over the live winner.

- **Phase 4 field tests** (2026-06-08): exercised `requiem-race-patching` on three real mods; each improved the skill (rules now in SKILL.md + memory `patching-preserve-author-intent`). **Aaron confirmed Phase 4 done.**
  - **HalfKhajiit.esp** (Ohmes-Raht playable, base + vampire) → Khajiit analogue. **Lessons:** (1) **never keep/add a `REQ_NULL_*` record** of any kind — inert stubs, only resolvable with Requiem as master (I wrongly kept `REQ_NULL_RaceKhajiitClaws 0AA01E`); (2) for a **playable** race, **strip the mod's own custom power spells** (a free heal/summon/vampire power becomes a free *player* ability) and `ReplaceAll` the `ActorEffect` to a true copy of the analogue (the vampire variant = a copy of the analogue's vampire). Patch `Authoria_Races_HalfKhajiit.esp`.
  - **Project ja-Kha'jay (Khajiit Diversity).esp** (10 furstocks) → all Khajiit. **Lessons:** (3) the "don't change a sensible author value unprovoked" rule is a **weapon/armor guard only** — for races, normalize *fully* to the analogue **including skill boosts**; (4) **classify humanoids by the `Playable` flag**, not the model path (these furstocks are NPC-only, `Playable` unchecked; the model/skeleton can be a red herring — a furstock is still a Khajiit). Patch `Authoria_Races_JaKhajay.esp`.
  - **DAc0da.esm** (47 creatures) → all classified. **Lessons:** (5) a creature needs the **WHOLE analogue copied** (Starting H/M/S, Regen, UnarmedDamage, skill boosts, Requiem keywords, **attack data via `AttackRace`=analogue**) + `req_trait_armor*`(+healing+flag), keeping its own `Ab*` resist — armor-only is under-patching; (6) **every creature MAPS — classify by Name + Keywords, never the EditorID** (recycled/misleading), with **Elite Draugr** (`DraugrRace 000D53` + `Armor_Draugr_Elite 03280F`) as the fallback; incorporeal spirits → `ghost` keyword `0D205E` (no armor). Construct keywords: `REQ_SneakAttackImmunity 4F6D1E`, `REQ_NoLifeDrainAllowed 2EA062`, `REQ_ActorTypeDaedraNoFlesh 44BE5E`; constructs/atronachs carry skill `Illusion 95` (fear immunity). Patch `Authoria_Races_DAc0da(2).esp`.
  - **houseCARL gotcha reconfirmed:** a just-written patch can **auto-activate and self-lock** (can't write into an active patch); author the next pass into a **fresh** patch, which copies the locked one as its base (inherits its edits) and supersedes it. (memory `housecarl-patch-rename-gotcha`.) **[SUPERSEDED — fixed in 1.2.1, see master note below.]**

- **Master note** (master session, 2026-06-08): **houseCARL updated 1.2.0 → 1.2.1.** The **active-patch write self-lock is FIXED** ("releases every mapped handle on the target before it saves") — Phases 5–9 should write **directly into active patches** via `into=`; ignore the fresh-inactive-patch workaround in the Phase-1/4 notes above. (The brand-new-patch-not-in-registry-until-refresh note still holds — verify a just-created patch via the `bulk_apply` read-back.) 1.2.1 also adds `cross_plugin_query where="Field op value"` value filtering (handy for Phase 5+ — e.g. `where="MagicSkill = Destruction"`) and `nexus_mod description=true`. Memory `housecarl-patch-rename-gotcha` trimmed accordingly. **Phase-4 MASTER ACTION done:** the race-domain authority exception (`Authoria - Master Patch - Races Merge.esp`) is now recorded in `docs/scope-and-authority.md`. Phase 5 (NPC) handoff emitted next.

- **Phase 5** (2026-06-09): authored `requiem-npc-patching` (SKILL.md 274 lines + 7 references + index.jsonl + eval set + 3 round-trips). Freshness re-verified (Iron Sword → `Requiem.esp`; resolver had been stale on `Requiem for the Indifferent.esp` at 3402 active — re-pointed to Authoria-dev, 2842 active). skill-creator invoked (§1). Template held (same shape as Phases 1–4). §8: hard items 1–15 + 19 pass (19 N/A — auto-discover); 16 pass (9+9 with near-misses); 17–18 on the §6.5 manual fallback (Windows blocks `run_loop`), predicted recall/specificity 1.0, flagged for empirical re-validation + peer check.

  **The central discipline — visual-vs-balance split:** `NPC_` records are contested by **appearance** winners (`MOSRefined.esp`, `TSOSRefined.esp`, NPC overhauls) AND **balance** winners (`Requiem.esp`, `USMP - Requiem.esp`, addons). The Reqtificator's `actorVisualAutoMerge` reconciles them. The patch sets **only balance fields**; use `conflict_tree` to separate the two and never write appearance. (Same carry-the-inputs discipline as the item domains.)

  **NPC authority:** balance resolves to `Requiem.esp` / **`USMP - Requiem.esp`** (Unofficial Skyrim Modders Patch – Requiem, an **in-scope** compat patch carrying Requiem's values forward — winner for ~128 named actors incl. Ahtar/Mjoll/Cosnach/Lydia) / Requiem addons (`Requiem - Minor Arcana - *`, MR, `Requiem_VampireCollection.esp`, WAR). **No active Authoria NPC merge** (`Authoria - NPC Merge` is DISABLED) — unlike the race domain, there is NO `Authoria - Master Patch` NPC winner to allow. 2702 NPC_ records touched by Requiem.esp.

  **Key derived rules (live, verify per record):**
  - **Level — fixed, de-levelled.** `Configuration.Level.Level` is a flat int (EncWolf 2, draugr 6, bandit tiers 3/7/12/19/24, dragon priest 50). **Patching a combatant = set fixed level + REMOVE `PCLevelMult`.** **Followers KEEP `PcLevelMult`** (Lydia is PcLevelMult, CalcMin6/Max50 — scales with player). The de-level rule is the core of NPC balance; followers are the one exception.
  - **Flags:** `AutoCalcStats` ON for humanoids (stats from level+class StatWeights+race); **OFF for creatures/bosses**, which carry explicit `HealthOffset`/`MagickaOffset`/`StaminaOffset` (EncTroll H+350/S+175; dragon priest M+1300). `Respawn` generic, `Unique` named/boss, `Protected`(+rarely `Essential`) followers/quest, `BleedoutOverride` bosses.
  - **Class is the balance spine.** Requiem assigns `REQ_Class_*` by role+weapon; the class `StatWeights` (H/M/S) drive AutoCalc (Bandit 4/0/6, Guard 5/0/5, **Slighted 1/0/0 = weak/trash tier**). Families: `REQ_Class_Bandit_{SwordShield 85BCE3,…,Trickster 8A8BE2 from Minor Arcana}`, `REQ_Class_Guard_*`, `REQ_Class_Slighted{1H/2H/Ranged/Conjurer}`, `FZR_Class_*` (cultist/ashspawn/hagraven/citizen). Combat style is mostly **vanilla reuse** + a few `REQ_CombatStyle_Guard_*`/`REQ_CS_*`.
  - **THE perk split (verified):** combat perks are **source-carried on the NPC** — vanilla `Skyrim.esm` skill perks (draugr 14, guard 17, bandit tiers 5→18) + Requiem skill-tree perks on stronger actors (`REQ_OneHanded_HandToHand`, `REQ_Conjuration_Empower_*`). The **`incomingDamageModifier` racial/state trait perk + the `gameMechanics` all-actor perks (armor pen/weight/arrow recovery) are Reqtificator-ADDED, ABSENT from source** — confirmed draugr 031285 not in its 14 source perks. **Never hand-stamp them.** (`ActorAssignmentRules_Requiem.esp.conf` is the spec.)
  - **ActorEffect = source-carried Requiem ability spells:** `REQ_Trait_Tempering_<role>_<tier>` (tempers worn gear; bandit `93369F`, guard `929824`) + `REQ_Trait_ResistMagic30 ADDDC7` etc. on casters/bosses. (Race trait spells `REQ_Trait_Armor/Resist/Healing_*` live on the RACE — engine-inherited — NOT on the NPC; don't copy them onto an NPC.)
  - **Template chain (clean integration):** `REQ_LookTemplate_*` (appearance; `TemplateFlags` inherit Stats/SpellList but omit Traits) → `REQ_<Role>_Template_<Weapon>_{Base,01,02,03}` base (carries class+perks+factions per tier; full bandit set = 8 weapons × 4 tiers) → deeper base. Template a new generic enemy onto the matching base + tick `Stats`+`SpellList` to inherit Requiem balance for free. Named bosses template onto the generic boss of their type (Rahgot→EncDragonPriestFrost) keeping own perks+trait.
  - **Four patch-classes:** generic combatant (template/replicate), creature (recognized race → touch almost nothing, race+Reqtificator carry it; new race → trait bridge), follower (record-side factions, keep PcLevelMult), **boss** (set fields directly — high fixed level, big stat offset, strong class, rich perks, a `REQ_Trait_*` reused [`ResistMagic30`] or bespoke; never template onto a generic base; under-buffing is the failure).
  - **Modded vs vanilla spells/perks:** modded (mod-defined, drives mechanics) → never remove, augment/patch; don't add a redundant usable spell if it already wields a modded one. Vanilla → replace with the Requiem analogue. (Generic resistances always fine to add.)
  - **Numbered NPCs:** Requiem's own `EncBandit01..06` are *intentional fixed-level tiers* (escalating level/perks/gear); a **mod's** arbitrary numbered leveling-copies (`BanditA01..05` feeding a LeveledCharacter) **collapse to one de-levelled tier**.
  - **Skip:** civilians/townsfolk/children/quest-helpers/vendors-no-combat + **conjured/summoned** actors. Identify by class (`Citizen 01326B`, `FZR_Class_Citizen`, job/vendor classes), AIData (Unaggressive), factions, child race. **Never patch `Factions`/`AIData`/mod-authored traits** (only follower factions are the record-side exception).

  **Round-trips ×3 (`evals/results-2026-06-09.json`):** (1) humanoid blind tier-extrapolation — derived bandit melee tier-05 from tiers 01/02/04: all constants exact, Level predicted ~18-19→actual 19, outfit 9336B0 exact, template 899DD8 sequential-exact, perks 18 in-range. (2) creature — confirmed draugr trait perk 031285 ABSENT from the 14 vanilla source perks (Reqtificator-added); creatures use offsets not AutoCalc. (3) boss — dragon priest Level 50 + MagickaOffset 1300 + `REQ_Trait_ResistMagic30` + `REQ_Conjuration` perk; Rahgot templates onto the generic boss keeping own perks+trait.

  **DECISION for the master / Phase 8:** **follower registration needs a Phase-8 script component.** The runtime registration of modded followers into Requiem's follower system is **script-side** — the mod `Authoria - Modded Follower Requiem Registration` is enabled but contributes **no active plugin** and ships `Scripts/Source` Papyrus. This skill does the record-side (PotentialFollower `05C84E` rank -1 + CurrentFollower `05C84D` rank 0, class/outfit/level-with-PcLevelMult/perks) and routes the runtime registration to Phase 8. The Phase-8 handoff should cover modded-follower registration.

  **Cross-cutting addition (Aaron-directed, 2026-06-09) — masters & REQ_NULL stripping:** authored `docs/masters-and-null-stripping.md` (now in the start-of-session checklist, governs ALL domains). Captures (a) the **xEdit master mechanism** we mirror — FormID = module-index byte + 3-byte local ID indexing the ordered `MAST` list (own records last); a plugin masters every file whose forms it references; Add Masters inserts `MAST` + renumbers (local ID untouched); Sort Masters load-order-orders + renumbers; masters must be load-order-sorted. **houseCARL does Add+Sort automatically from referenced forms** (verify the `masters:` read-back); force `Requiem.esp` in by referencing a real Requiem form (no empty-master verb). (b) **REQ_NULL stripping is now universal** (generalizes the Phase-4 race rule to every domain): `REQ_NULL_*` are inert retired stubs; houseCARL reads live so it **resolves their EditorIDs** — scan `Perks`/`ActorEffect`/`Keywords`/inventory/`Template`/leveled-`Entries` and **strip every one; none may remain**. Folded into the NPC skill (SKILL.md Checklist + Common-mistake; `housecarl-recipes.md` "Masters & REQ_NULL stripping"; index.jsonl). **MASTER ACTION for Phase 9 (and a propagation pass):** replicate the same Checklist item + Common-mistake + recipe section into the other skills (weapon/armor/ammo/race) and add a Phase-9 audit gate — *no emitted patch may leave a `REQ_NULL_*` reference, and every patch's `masters:` list must be correct/load-order-sorted*.

  **Field test (2026-06-09) — patched `ksws07_quest.esm` (7 NPCs):** correctly **skipped 5** (skeever already `Stats`+`SpellList`-templated onto the vanilla skeever; "Ghost" Siwe = invulnerable/unaggressive quest NPC; 3 "Dead" dungeon corpses) and identified the two combatants. **Boss "Cursed Shadow" (002170)** patched into `Requiem NPC patching.esp`: recognized as a ghost-themed boss (`DoesNotBleed`, `NoSoulTrap`, modded "Ghost Ability") → set fixed **Level 50** (dragon-priest boss tier), added the vanilla **`ghost` keyword 0D205E** so the Reqtificator assigns the ghost incoming-damage trait (physical resistance, magic counterplay), and **removed** an initial `REQ_Trait_ResistMagic30` because it would undercut the ghost's intended magic-vulnerability (studied self-correction — a ghost shouldn't resist its own counter). Kept the modded ability, 20 perks, Essential/Unique. **Waifu "Othriel" (002039)** = quest-only companion, appearance owned by `Snowfall…Replacer.esp` → **left untouched** (balance already Requiem-reasonable; don't clobber appearance or force follower factions on a non-recruitable). Confirms the visual-vs-balance split + the four-class identification in practice. (Aaron feedback captured in memory `be-decisive-master`: make studied calls, don't over-ask.)

  **Reuse for later phases:** the source-vs-assigned split now has a fourth shape — **source-carried (combat perks + tempering/resist trait spells on the NPC) vs Reqtificator-assigned (trait + gameMechanics perks, by race/keyword/all-actor)**. The trait bridge from Phase 4 is consumed here (recognized race → nothing; new race → retarget Race or hand-add physique perk). DeathItem/outfit **contents** → Phase 6; spell/MGEF **design** → Phase 7; follower-registration **runtime** + any script hooks → Phase 8.

- **Phase 6** (2026-06-09): authored `requiem-leveled-list-patching` (SKILL.md 215 lines + 5 references + index.jsonl + eval set + round-trip with live writes). Freshness re-verified (Iron Sword → `Requiem.esp`, chain Skyrim→USSEP→Requiem). skill-creator invoked (§1). Template held (same shape as Phases 1–5). §8: hard items 1–15 + 19 pass (19 N/A — auto-discover); 16 pass (9+9 near-misses); 17–18 on the §6.5 manual fallback (Windows blocks `run_loop`), predicted recall/specificity 1.0, flagged for empirical re-validation + peer check.

  **This is the PLACEMENT/DISTRIBUTION layer** — it links already-statted records (Phases 1–3 items, Phase 5 actors) into the world; it does **not** re-stat. Four record types: LVLI, LVLN, CONT, ECZN. Counts Requiem touches: **3433 LVLI, 571 LVLN, 279 CONT, 8 ECZN.**

  **THE merge mechanic (definitive, read from Reqtificator source `Transformers/LeveledLists/` + 8 unit tests, not guessed):** `mergeLeveledLists`+`mergeLeveledCharacters`+`openEncounterZones` all true (base + Authoria `DefaultUserConfig.json`, identical).
  - **Add-only by consensus.** Merge = Requiem base ∪ (all contributors' ADDITIONS, unioned) − (removals/modifications that EVERY contributor agrees on). Entry identity = the `(Reference, Level, Count)` tuple. You can freely ADD; you cannot unilaterally remove/change a Requiem entry. 255 cap.
  - **Eligibility = Requiem + plugins that master `Requiem.esp`** (per PLUGIN). **Trigger:** Requiem must define a base version AND there must be **≥3 eligible candidates** (Requiem + ≥2 others) — else the plain conflict **winner** is used verbatim. In this load order many core lists are touched only by Requiem (+WAR), so the merge often does NOT fire → the winner is taken as-is.
  - **Robust patch shape = `verb=Add` to the winner** (houseCARL overrides the winning record, which holds all Requiem's curated entries, and appends yours → record = base + addition). **Dual-correct:** merge fires → your entry unioned (no dup, additions only take non-base tuples); merge doesn't fire → winner = base+addition. Emitting a bare lone-entry list would WIPE Requiem's curation when merge doesn't fire — the key failure to avoid.

  **EMPIRICAL FINDING (live writes, important) — the master gotcha is real and per-plugin.** Most core `REQ_LI_*` lists are *renamed but still `Skyrim.esm`-defined* (`REQ_LI_Loot_Weapon_Sword` = `016578:Skyrim.esm`). A live `Add` of a vanilla item (steel sword `013989`) to `016578` came back `masters: Skyrim.esm, Update.esm, Dragonborn.esm` — **Requiem.esp ABSENT** → that patch is dropped from the merge and its addition can be LOST. **Fix (verified):** add one edit referencing a Requiem-DEFINED form — e.g. `Add` to `REQ_LI_LootMidLevelContent 050EF1:Requiem.esp` — and masters flipped to include `Requiem.esp` for the WHOLE patch (per-plugin). Baked into SKILL.md step 4 + Common mistakes + `merge-behavior.md`.

  **De-leveling model (core finding):** **every Requiem list entry is `Level = 1`** (verified across LVLI Loot/Town/Best + overridden vanilla list `LItemEnchWeaponDaggerBlacksmith15` + LVLN `LCharWolf`). Tier is encoded by **which list** (`REQ_LI_{Loot|Town|Best|Blacksmith|Special}_{Weapon|Heavy|Light}_{Subtype}` — Loot=dungeon broad, Town=vendor basics, Best=high-end) + **repetition** (iron sword 3× in the loot list, daedric once), NOT by the Level field. Refines the handoff's "match Requiem's tier for the Level" — Level stays 1; weight by repetition + list choice. Flags `CalculateFromAllLevelsLessThanOrEqualPlayer, CalculateForEachItemInCount`, `ChanceNone 0`.

  **Containers:** Requiem curates boss chests (`TreasDraugrChestBoss` = 10 items, trimmed from vanilla 18 / LotD 23) by **referencing themed leveled lists** (`LootDraugr{Gems,Potions,SoulGems,Jewelry,Scrolls}`, weighted by repetition) + its own generic pools (`REQ_LI_LootMidLevelContent`, `REQ_LI_TreasureHunter_<Theme>`). Gear comes from the enemy death-item/outfit (Phase 5), NOT stuffed in the chest. **CONT is NOT merged** (only LVLI/LVLN are) — a container override replaces `Items`, so build on the winner.

  **Encounter zones (correction to the handoff):** `openEncounterZones` only sets the `DisableCombatBoundary` flag (verified in `OpenCombatBoundaries.cs` + test) — it does **NOT** change MinLevel/MaxLevel. Requiem's de-leveling is the fixed-level NPCs (Phase 5) + Level-1 lists, not the zone. New-area ECZN normally needs **nothing**; only set MinLevel if a modded zone hard-gates against Requiem's flat design. Requiem ships only ~8 ECZN overrides (DLC2 dungeons MinLevel 25 + NeverResets).

  **REQ_NULL-in-lists nuance (resolved):** Requiem deliberately keeps `REQ_NULL_*` entries and retired `REQ_NULL_*` sublists (e.g. `REQ_NULL_LootDraugrEnchWeapons100`, `REQ_NULL_SubCharBandit01..06_*`). Additive patching emits only YOUR new entry, so there's nothing of Requiem's to strip — and you must NOT strip its intentional NULL removals. The universal strip rule applies to what you INTRODUCE (never add to a `REQ_NULL_*` list, never add a NULL reference). Only a full list override strips NULLs from the copy.

  **Reqtificator OUTPUT lists — don't author (new "output" shape):** lists named `<prefix>_..._Quality<N>_<H|N|D>_<fall|const|rise>` are tempered-quality seeds the Reqtificator expands into `ItemCondition` variants at build (`TemperedItemGeneration.cs`). Like the weapon damage-type keyword — an output, not source; don't hand-author. (Compact `*_CLI_` lists: `Count` = multiplicity, unrolled before merge.)

  **Authority (placement):** mostly `Requiem.esp` + WAR, with Requiem addons (Minor Arcana, MR) winning themed pools. Independent overhauls that do NOT master Requiem (LegacyoftheDragonborn, trade & barter, Sons of Skyrim, New Legion, Dawnguard Arsenal) win some lists by load order but are **invisible to the merge** — read the live winner per record. **No Authoria leveled-list master-merge active** (unlike the race domain's Master Patch) — verified, not assumed.

  **Round-trip:** (read) reverse-referenced the steel sword `013989` → recovered its full list footprint (Loot/Town/Blacksmith/Special/faction/reward); the additive recipe reproduces that placement exactly. (write, live, into `Requiem leveled list patching.esp`) `Add` steel sword to `016578` → 20→21 entries (base preserved); then the master-gotcha proof above. `evals/results-2026-06-09.json`.

  **Reuse for later phases:** the source-vs-assigned split now has a placement shape — **placement is add-only; you carry the entry, the merge assembles** (when eligible). The per-plugin master requirement is a placement-specific hardening of the masters doc. The "don't author Reqtificator output lists" rule (tempered-quality seeds) is the leveled-list analog of "don't hand-stamp the damage-type keyword." Verification artifact left as inactive patch `houseCARL - Requiem leveled list patching` (not enabled in MO2).

- **Master note** (master session, 2026-06-09): Phase 7 (`requiem-magic-patching`) handoff emitted (`docs/handoffs/phase-7-magic.md`) — MGEF/SPEL/ENCH design, Magic Redone authoritative. Flagged the **three-layer magic split** (record-side design / Reqtificator rescaling perks / Magic Redone `Nox_*` script runtime) and directed mining the **existing MR-patch addons** (`Requiem - Constellation/Obscure/Dark Hierophant/Holy Templar/Sonic/Wildwaker Magic - Magic Redone Patch.esp`, Apocalypse compat) as real "new magic patched for Requiem" worked examples. Consumes the Phase-1 staff frame + Phase-3 elemental-arrow effect. **Phase 7 must report what Phase 8 needs to cover for the `Nox_*` runtime** — feeds the Phase-8 script handoff alongside the already-flagged follower-registration scripts.

- **Master note** (master session, 2026-06-09): Phase 6 (`requiem-leveled-list-patching`) handoff emitted (`docs/handoffs/phase-6-leveled-lists.md`) — the placement/distribution layer (LVLI/LVLN/CONT/ECZN). Central mechanic flagged: the **Reqtificator merges leveled lists** (`mergeLeveledLists`/`mergeLeveledCharacters` ON) → patch **additively** (add entries at the right tier), don't fully override; plus the **REQ_NULL-in-lists nuance** (Requiem's own intentional `REQ_NULL` list removals are NOT stripped — only our emitted entries must be NULL-free). Carries the `be-decisive-master` directive forward. **Tracking two flagged actions for me:** (1) **Phase 8** handoff must cover **modded-follower runtime registration** (script-side; the record-side is done in Phase 5). (2) **Phase 9** propagation + audit gate — back-fill the `masters-and-null-stripping` Checklist item + Common-mistake + recipe into the weapon/armor/ammo/race skills, and add a Phase-9 gate: *no emitted patch may leave a `REQ_NULL_*` reference; every patch's `masters:` list must be correct + load-order-sorted.*

- **Phase 7** (2026-06-09): authored `requiem-magic-patching` (SKILL.md ~290 lines + 7 references + index.jsonl + eval set + round-trip with live write). Freshness re-verified (Iron Sword → `Requiem.esp`; a known magic record → `Requiem - Magic Redone.esp`). skill-creator invoked (§1). Template held (same shape as Phases 1–6). §8: hard items 1–15 + 19 pass (19 N/A — auto-discover); 16 pass (10 should-trigger + 9 near-miss); 17–18 on the §6.5 manual fallback (Windows blocks `run_loop`), predicted recall/specificity 1.0, flagged for empirical re-validation + peer check. **Scope grew per Aaron mid-phase (twice):** folded in **spell tomes (cost-by-tier), the full magic perk system, and the spell-tome leveled-list/perk relationship**; then a **full sub-spell archetype taxonomy** (`references/spell-archetypes.md`, the 7th reference) — ~50 archetypes across the 5 schools, since the basic-element sampling missed Destruction **Venom/Arcane/Entropic/Absorb**, the Restoration **Sun/Poison/TurnUndead/Dispel/WeaknessMagic** lines, the Conjuration **Bound/Daedra/Undead/Reanimate/Spirit/Banish/SoulTrap/Oblivion/Necrotic/Teleport** families, the Illusion **mind/Shadow/Sound/stealth** set, and the Alteration **Armor/Shield/Telekinesis/Paralyze/Physical/Weakness/Transmute/Wind/Ash/Enlarge/Etherealize** set. **Effect-types live in multiple schools** — poison = Destruction `Venom` + Restoration `Poison` + alchemy; undead = Conjuration raise + Restoration Turn/Sun. Classify the spell to its **archetype** (not just school), then read that archetype's same-tier comparable.

  **Authority:** `Requiem - Magic Redone.esp` (MR) for spells/effects/enchants/staves/perks/scrolls; base `Requiem.esp` for shouts; `Requiem - Alchemy Redone.esp` for potions/poisons. Some spell perks resolve to `Requiem - MR Special Feats Patch.esp` / `Requiem - MR WAR Resist and Regen Tweak Patch.esp`. **The MR-patch addons are the worked examples** — `Requiem - Constellation/Obscure/Dark Hierophant/Holy Templar/Sonic Mage Magic - Magic Redone Patch.esp`, `Wildwaker Magic - Requiem Magic Redone Patch.esp`, Apocalypse compat — each rebalancing a new spell pack (Supernova/Sonic Mage deltas mined).

  **Key derived rules (live, verify per record):**
  - **Cost is EXPLICIT, no formula.** SPEL `Flags = ManualCostCalc` → read `BaseCost` directly; there is **no magicka-cost formula** in the Reqtificator config (the handoff's "Layer-1 underivable cost formula" does not exist as a computed function). Cost is per-(school, delivery, tier): Destruction 40/90/240/495; Conjuration far higher (Bound 150 / Daedra 500 / Reanimate 800); Alteration/Illusion utility cheap (25–50); wards cheap (60). **Derive from the same-school same-delivery same-tier comparable, not a universal ladder.**
  - **`HalfCostPerk` = THE school+tier classifier.** It names a `REQ_<School>_Mastery_<NNN>` perk that sets the spell's **school + tier**, gates **learning**, grants half-cost. Matrix (all `:Skyrim.esm`, winners MR / MR Special Feats Patch): Destruction `0F2CA8/0C44BF/0C44C0/0C44C1/0C44C2`; Restoration `0F2CAA/0C44C7/0C44C8/0C44C9/0C44CA`; Conjuration `0F2CA7/0C44BB/0C44BC/0C44BD/0C44BE`; Alteration `0F2CA6/0C44B7/0C44B8/0C44B9/0C44BA`; Illusion `0F2CA9/0C44C3/0C44C4/0C44C5/0C44C6` (Novice→Master). The MR-patch addons **reclassify a spell's school** by re-pointing it (sonic→Alteration, star→Restoration).
  - **ChargeTime ladder (clean):** T1 conc 0 / T1 FaF 0.25 / T2 0.5 / T3 0.75 / T4 1.0 / ritual 3+. Requiem uses ChargeTime as the **anti-spam lever** on big spells.
  - **Spell = multi-effect.** Primary damage/heal effect (MR **reuses the vanilla MGEF FormID**, overridden to set school/resist/keywords/flags) + a secondary (slow/stagger) + a taper/grandmaster effect (MR-defined, **dummy ActorValue `Fame` + `HideInUI`** when mechanic-only). Each `Effect` has its own `Data` (Magnitude/Area/Duration) + `Conditions` (perk gate). Base magnitude scales at runtime via the engine `PowerAffectsMagnitude` flag — set the *base*, the tier value.
  - **Keyword vocab:** `MagicDamageFire 01CEAD` / `Frost 01CEAE` / `Shock 01CEAF` / `Resist 0F81E3` (Skyrim.esm); `REQ_SpellConcentration 2FFEAD`, `REQ_NoDurationScaling 412EDF` (Requiem.esp); `Nox_KW_Staff_<School><Tier> 0076E1–0076F9` (MR, 5×5). MGEF `Flags` (Hostile/Detrimental/FXPersist/NoDeathDispel/**PowerAffectsMagnitude**/NoArea/IgnoreResistance/NoAbsorbOrReflect) are load-bearing — mirror the comparable. Set `MagicSkill` (school AV) + `ResistValue` (ResistFire/Frost/Shock/Poison/MagicResist).
  - **THE three-layer split (the magic per-domain split):** (1) **record-side** = MGEF/SPEL/ENCH design (author it). (2) **Reqtificator-assigned `RFTI_All_*` rescaling perks (NEVER hand-stamp):** `RFTI_All_AbsorbRescaling 962799`, `RFTI_All_PoisonRescaling 962798`, `RFTI_All_Magic_WardDamageReduction 682FB5`, `RFTI_NPC_PersistentSpellRescaling AD3977` — the `RFTI_` prefix marks the whole assigned family (incl. `RFTI_Trait_*` creature traits). (3) **`Nox_*` script runtime (Phase 8):** cover **special mechanics only** (bound items, teleport, mind-control, weather, scroll-craft, multi-spell tome, weakness/rune) — **base damage/heal/buff scaling is the engine `PowerAffectsMagnitude` flag, NOT a script**, so a plain spell needs no Papyrus.
  - **Spell tomes (BOOK):** `Type=BookOrTome`, `Teaches`=SPEL, Weight 1, standard `InventoryArt`. **Requiem normalizes tome `Value` to a tier ladder — Novice 100 / Apprentice ~300–400 / Adept 600 / Expert 800 (≤1000) / Master 2000** (identical across every MR-patch addon; Supernova tome 4975→2000). No learn-gate field — learning is gated by the spell's `HalfCostPerk` tier. **Tome PLACEMENT into vendor/loot lists → Phase 6** (the "leveled-list tied to perks" concern = LVLI placement is Phase 6; the perk tie is the learn-gate, automatic once the tier perk is right).
  - **Perks: hook into existing trees, don't author new.** A new spell joins a school via its `HalfCostPerk` + the school's specialization keyword (Pyromancy/Cryomancy/Electromancy; Necromancy/Daedric/Bound/Spirit; MagicResistance/MageArmor/Metamagic; Perceptual/Emotional/Delusive/ShadowMagic; Healing/Recovery/Turning/Protection; Enchanting). Specialization perks **gate secondary MGEF** (Cremation effect `005AA8` ↔ `REQ_Destruction_Pyromancy_050_Cremation 0F392E` via `PerkToApply`/Conditions) — copy the gated effect verbatim. Rebalance a mod's own perks to Requiem conventions only if it ships them.
  - **Enchantments:** **weapon** (`EnchantType=Enchantment`, FaF/Touch) per-cast `EnchantmentCost` by tier ≈10/20/25/35/40/50, charge **pool** = WEAP `EnchantmentAmount` ≈500×tier (frame=Phase 1); Frost/Shock are DualValueModifier (+slow `0B72A0` / +dwemer `19DC31`). **Apparel** (`ConstantEffect`/Self) no charge pool, magnitude on the MGEF. **Staff** = WEAP `EnchantmentAmount`+`Nox_KW_Staff_<School><Tier>`; the `ObjectEffect` casts the school's spell effect at tier power.
  - **Elemental projectile:** the hit lives on the **explosion**, not weapon/ammo damage — AMMO → PROJ(`Explosion`) → EXPL(Force/Radius) → ObjectEffect/MGEF (`REQ_Ench_Ammo_Fire01 AD3818` → effect `04605A`). Design the EXPL→MGEF; AMMO/PROJ link = Phase 3.
  - **Alchemy (folded in):** MGEF Archetype `PeakValueModifier`, FaF/Self, `Flags=PowerAffectsMagnitude,Recover`, `BaseCost≈0.025`, `MagicSkill=None`, keywords `MagicAlch*` (`MagicAlchHarmful`=poison). `RFTI_All_PoisonRescaling 962798` (Layer 2) rescales harmful alchemy.
  - **REQ_NULL (274 effects):** retired vanilla abilities/powers/perk-effects (`REQ_NULL_AbResistFire 024314`, `REQ_NULL_RaceOrcBerserkEffect`, etc.). **Strip on player-side spells/items; on a creature/NPC, a NULLed vanilla resistance/power was replaced by a Requiem race trait spell → route the replacement (Phase 4/5), don't bare-delete the protection.**
  - **Scrolls (665 in MR):** `REQ_Scroll_<School><Tier>_<Type>_<Delivery>`, Weight 0.5, modest tier-scaled value, charge mirrors the spell, same MGEF. **Shouts** (SHOU, owned by `Requiem.esp`): 3 Words of Power → 3 SPEL + recovery; niche.

  **Round-trip (exact + live write):** reproduced `REQ_Destruction2_Frost_Aimed` (Ice Spike) from the `REQ_Destruction2_Fire_Aimed` (Firebolt) comparable — cost 90 / FaF-Aimed / charge 0.5 / `HalfCostPerk 0C44BF` / 3-effect shape (primary mag 30 + secondary + taper) all exact, only the element-specific MGEFs swapped. Authored from scratch via `create_record` into `Requiem magic patching.esp` (Spell `000800`): all fields read back exact, **masters auto-resolved to `Skyrim.esm, Update.esm, Requiem - Magic Redone.esp`** (MR pulled in by a single MR-effect reference), no `REQ_NULL`. Validated the `Effects` compose shape end-to-end. (`evals/results-2026-06-09.json`.)

  **Full MR content survey (Aaron-directed re-analysis, 2026-06-09 — `references/mr-content-survey.md`, the 8th reference):** analyzed EVERY record class MR touches, not just spells. Counts: **MGEF 1413** (~550–600 MR-defined), **ENCH 1029**, **COBJ 2955**, **WEAP 1250**, **SCRL 665**, **BOOK 584**, **ARMO 340**, **PERK 186**, **KYWD 107**, **PROJ 87**, LVLI 327, FLST 51, **EXPL 40**, **HAZD 39**, ALCH 24, ARTO 22, MISC 11, AMMO 6, RFCT 3, DUAL 1 (LVSP/SHOU/QUST = 0). Findings folded into the references:
  - **New damage types (the "new magic effects"):** beyond Fire/Frost/Shock, MR adds **Venom** (Destruction, `PoisonResist`+`REQ_PoisonSpell AD3904`), **Poison** (Restoration, `PoisonResist`), **Arcane** (Destruction, **unresistable** — no `ResistValue`), **Entropic** (Destruction, `ResistMagic`), **Sun** (Restoration, `ResistMagic`+`REQ_SunDamage ADDDF6`), **Necrotic** (Conjuration, `ResistMagic`), **Absorb H/M/S** (`Absorb` archetype + `REQ_Absorb ADDDF7`). Each tagged with a `Nox_KW_<School>_<Type>` keyword. Damage spell shape = **primary + `*_Taper` (lingering DoT, dummy `Fame` AV + `HideInUI`) + perk-gated GM** (Cremation etc.).
  - **New enchantments:** **1029 ENCH, all `NoAutoCalc`** — **Staff 576** (`StaffEnchantment` type, wraps the equivalent spell's MGEF at the spell's magnitude, cost 100–300, craftable two ways), **Armor 300** (`ConstantEffect`, tiers 01–06, `BaseEnchantment` template + `WornRestrictions` on `_Base`, cost 100×tier; ladders: resist 10/20/25/35/40/50%, fortify-attribute 20/30/50/70/80/100, fortify-skill +4@01; MR-new ArmorPenetration/SneakAttack/**SpellAbsorption**), **Weapon 105** (charge-based, single-effect Cost=Amount=Magnitude on the 10/10/20/25/35/40/50 t0–6 ladder; Frost/Shock dual, Chaos/Force triple; MR-new Toxicity/Arcane/bursts), Bound 13, Ammo 12, Artifact 2.
  - **Crafting systems (COBJ 2955):** three furniture families — **staff forge** (`CraftingSmithingForge`, HasPerk+HasSpell, blank+soul gem), **DLC2 staff enchanter** (HasSpell only, blank+2 heart stones), **scroll crafting** (`CraftingScrollCraftingTool`, paper roll + 2 soul-gem fragments). A COBJ assembles an already-enchanted record gated on knowing the spell; it never embeds the effect.
  - **Pre-enchanted gear:** MR factors enchanted items as **{base item} × {ENCH} × {magnitude tier}** — each a distinct ARMO/WEAP override (armor=no charge, staff=charge ~1000). To add a new enchanted item: point its `ObjectEffect` at an ENCH, add a COBJ to craft it, an LVLI to place it.
  - **Delivery/FX chain:** MGEF wires `Projectile`/`Explosion`/`Hazard`. PROJ profiled by `Type` (Cone/Beam/Missile). **EXPL `Damage=0`** — it's a force+visual shell; AoE damage comes from the MGEF area. **HAZD** (walls/runes/circles) re-applies its `Spell` link on a tick (HAZD→SPEL→MGEF). FX records: ArtObject (cloak/hand casting art), DualCastData (magelight).
  - **Keyword vocab (107):** the full `Nox_KW_*` namespace catalogued + classified **record-side** (staff/enchanting/physical-tier) vs **Nox-runtime** (school-mechanic, perk-gate `Nox_KW_<School>_Perk_<Tree>`, Touch/Nova/CloakDamage). The `REQ_PoisonSpell/SunDamage/Absorb` damage markers are `Requiem.esp`-defined, used by MR. (`keywords.md` expanded.)
  - **FLST/LVLI/MISC/ALCH supporting layer:** spell pools `REQ_List_<School>_<Tier>Spells`, robe/staff distribution lists, the scroll reagent economy (paper roll + soul-gem fragments), and Fortify-skill potions (overlap Alchemy Redone).

  **For Phase 8 — what the script handoff must cover for the Magic Redone `Nox_*` runtime** (the magic Layer-3 boundary; scripts live in `Requiem - Magic Redone - rerun\scripts\`, ~50 magic `Nox_*.pex`):
  1. **The split is clean: base scaling is NOT scripted.** Damage/heal/buff magnitude scales through the engine `PowerAffectsMagnitude` flag; the core damage MGEF carry **no `VirtualMachineAdapter`**. So a plain new spell needs **zero** Papyrus — Phase 8 only handles spells with a special mechanic.
  2. **The special-mechanic scripts to document** (which MGEF/spell archetype each attaches to, the properties it needs, and the marker keyword a new spell must carry to be picked up): **Conjuration** — `Nox_BoundWeapon`/`Nox_BoundArmor`/`Nox_Conjuration_BoundThrowingKnives` (bound items), `Nox_Conjuration_Teleport`/`Blink`/`GreaterTeleport`, `Nox_Conjuration_SiphonCorpse`/`DarkSacrifice`/`MysticInfusion`, `Nox_Enthrall`(+`_Dispel`); **Illusion** — `Nox_Illusion_Command`/`Control`/`Frenzy`/`Sleep`/`Silence`/`Vanish`/`Death`/`Break`/`Hallucination`/`ShadowClone`/`ShadowDoor`/`ShadowStep`/`VeilOfShadows`/`SetAlpha`/`AbsorbCheck`/`RecastDispel`, `Nox_Charm`; **Alteration** — `Nox_Alteration_Jump`/`Open`/`ControlWeather`/`TelekineticDisarm`, `Nox_EnlargeShrink`; **Restoration** — `Nox_Restoration_Purify`/`DivineIntervention`, `Nox_Dispel`; **Destruction** — `Nox_Destruction_Weakness`, `Nox_Destruction_RuneMastery`; **Enchanting/tomes/scrolls** — `Nox_Enchanting_ArtifactEnchanter`/`MultipleEnchantments`/`ScrollCraftingFurniture`/`ScrollCraftingTool`, **`Nox_TomeMultipleSpells`** (a tome teaching >1 spell).
  3. **The `Nox_KW_Staff_<School><Tier>` keyword → runtime mechanism:** staves carry the keyword (frame = Phase 1, effect = Phase 7); how the Nox runtime reads it to scale staff charge/power is the script side to document. (No staff-scaling `.psc` surfaced by name in the magic script set — Phase 8 should locate the quest/MGEF that consumes the staff keywords, likely the `Nox_WAR_Quest`-style control quest analog for magic.)
  4. This is **alongside** the already-flagged modded-follower runtime registration (Phase 5 did the record-side). **Phase 8 NOT started.**

- **Master note** (master session, 2026-06-09): **Phase 8 EXPANDED per Aaron into the capstone** — `docs/handoffs/phase-8-scripts-and-capstone.md`. It authors **two** skills: (1) `requiem-script-patching` (the Nox runtime per Phase-7 spec + modded-follower runtime registration + REQ_ scripts + VMAD attach / `compile_script`), and (2) `requiem-patching` — the **integration brain/router** that encodes Requiem's full vision + every mechanic and why, produces the complete "make plugin X play flawlessly with Requiem" plan (enumerate records → route to the 7 domain skills → final masters/REQ_NULL/Reqtificator checklist), and **fills the gap mechanics**: vampirism & lycanthropy (headline), diseases, exhaustion/stress, stealth/lockpicking/pickpocket, alchemy, food, shouts, standing stones/birthsigns, perks/skills/leveling, economy, and the core combat/resistance model. Each gap = a `references/<mechanic>.md`; vampire/lycanthropy MAY be promoted to its own skill. **Consequence for Phase 9:** the router is now authored in Phase 8, so **Phase 9 (master) = the masters/REQ_NULL back-fill into all skills + the §8 audit of all 9 skills + the no-REQ_NULL/masters-sorted gate + packaging** (no router authoring). Derive-don't-invent: any gap not fully resolvable live is flagged for Phase 9 to finish.

- **Phase 8** (2026-06-09): authored BOTH capstone skills. Freshness re-verified (Iron Sword → `Requiem.esp`, chain Skyrim→USSEP→Requiem, depth 3; 2842 active). skill-creator invoked (§1). The project is now **9 skills**. §8 for both: hard items 1–15 + 19 pass (19 N/A — auto-discover); 16 pass (10+10 with router-vs-domain near-misses); 17–18 on the §6.5 manual fallback (Windows blocks `run_loop`), predicted recall 1.0 / specificity 0.8–0.9, flagged for empirical re-validation + peer check. `requiem-script-patching` SKILL.md 180 lines + 5 references + index.jsonl + eval set; `requiem-patching` SKILL.md 131 lines + 14 references + index.jsonl + eval set + dry-run results.

  **Part A — `requiem-script-patching` (the Papyrus layer):**
  - **THE central rule — the script-vs-no-script gate.** Base damage/heal/buff/ward magnitude is **never script-driven** — it scales via the engine `PowerAffectsMagnitude` flag; the Reqtificator assigns the `RFTI_All_*` rescaling perks. A script is needed **only for a special mechanic**, and the right move is almost always to **reuse Requiem's existing script + its marker**, not author a `.psc`. **VMAD-split nuance (corrected live):** "plain effects have no VMAD" is *not* categorical — many do (bound-weapon effects `01CE9F`/`005DE7` carry none), but a Requiem effect that **reuses a vanilla MGEF FormID** can inherit an **incidental vanilla FX/quest script** (Firebolt `012F03:Skyrim.esm` carries `MG01FireEffectScript` AND keeps `PowerAffectsMagnitude`). That incidental script is not a mechanic you add/replicate/remove and is not what scales damage. The gate is unchanged: a plain new spell needs no script of yours.
  - **Nox runtime map** (`references/nox-runtime-map.md`): the ~45 `Nox_*` scripts (loose SOURCE at `…\Requiem - Magic Redone - rerun\scripts\source\Nox_*.psc`; `.pex` alongside — NOT BSA-packed) mapped by mechanic → record it attaches to → properties → marker. Almost all extend `ActiveMagicEffect`/`OnEffectStart` (attach to MGEF); `Nox_TomeMultipleSpells` is the exception (extends `ObjectReference`/`OnEquipped`, attaches to the BOOK, fills `SpellsToLearn[]`). Bound: `Nox_BoundWeapon` (`BoundAbility[]` + `EffectIndex` global + `MysticInfusion` perk gate). Live worked example (verified, the round-trip): `REQ_Effect_Conjuration4_Teleport_RitualSelf 02CBCE:Requiem.esp` → VMAD `Nox_Conjuration_Teleport` (Flags=Local, Version 5, ObjectFormat 2), 4 props `Mark 02CBC8`/`Park 02CBCF`/`XarrianCell 02900B`/`SummonFX 07CD55:Skyrim.esm`. Clone-and-repoint is the reuse recipe.
  - **Staff-keyword finding (corrects the Phase-7 expectation):** there is **NO** `Nox_KW_Staff` script consumer. Staff power/charge scaling is **record-side only** (`EnchantmentAmount` + the ObjectEffect magnitude + the keyword as a *classifier*). The only "staff array" reader is `Nox_Conjuration_BoundThrowingKnives` (`Weapon[] SourceStaff`, to pick the equip slot) — not a keyword reader. A plain staff needs no script; don't hunt for a staff-scaling `.psc`.
  - **Follower registration** (`references/follower-registration.md`): the API is `REQ_FollowerRegistration.RegisterFollowerAlias(ReferenceAlias)` (the `REQ_FollowerControl` instance; storage **30 alias slots**, slot 0 = vanilla follower; the alias must be in `playerFollowerFaction` to count as *current*). The reference impl is `_Authoria_REQ_MajorFollowerBridge` (a Quest with one ReferenceAlias per major follower calling RegisterFollowerAlias on init, retrying). **Per Aaron's directive: if the plugin contains a follower, add it to Authoria's registration quest** (add a ReferenceAlias on the follower → the bridge registers it). **Load-order caveat (verified live):** the bridge mod is enabled (loose script present) but its ESP `Authoria - Master Patch - Requiem Follower Registration.esp` is **INACTIVE** → the bridge is currently dormant; flag this when registering a new follower.
  - **REQ_* core scripts** (`references/req-core-scripts.md`): surveyed the load-bearing ones (`REQ_CoreScript`, install/version quests, `REQ_AttributeSystem`, `REQ_MCM`, `REQ_FollowerRegistration`, `REQ_LockpickControl`, magic/disarm/disguise) — never override/detach; new content **registers** (keyword/faction/alias), doesn't replace. Diseases = a `Touch`/`ConstantEffect` ability SPEL whose single **MGEF** carries the staging (often a small MGEF script), not the SPEL.
  - **VMAD + compile** (`references/vmad-and-compile.md` + `housecarl-recipes.md`): the VMAD field structure (Scripts/Properties/Version 5/ObjectFormat 2) as read = the write schema; clone + repoint + read-back. `housecarl_compile_script` path documented (CK `PapyrusCompiler.exe`, auto-imports, `import_dirs=`, `into=`); **not exercised** (optional — would need the CK compiler path set; flagged optional). Used `papyrus-reference`/`papyrus-optimization` as the style authorities.

  **Part B — `requiem-patching` (the integration brain/router):** hybrid of the §5.2 router archetype + an operational integration workflow (justified in-body per §8 item 14). Body = Overview → First step (freshness + `cross_plugin_query plugins=["<mod>.esp"]` to enumerate) → integration workflow → routing table → gap routing → checklist. References (14): `requiem-vision.md` (the 5 pillars + the keyword-driven/Reqtificator-assembled model), `combat-resistance.md` (the armor-pen/resistance/stagger/exhaustion chain with FormIDs — armor pen slash `AD394D`/blunt `AD394E`/pierce `AD394C`/ranged `AD3948`; magic-resist cap 60/race ×3), `routing-table.md`, `integration-checklist.md` (the cross-cutting gate + **the Reqtificator-handles-this vs needs-manual matrix** rolled up across all domains), and the 11 gap mechanics (vampirism-lycanthropy, diseases, exhaustion-stress, stealth, alchemy, food, shouts, standing-stones, perks-skills, economy).
  - **Gap-mechanic spine (from `ActorAssignmentRules_Requiem.esp.conf`, full transcription):** all-actor `gameMechanics.perks` (armor pen ×4, armorWeight `AD3A34`, arrowRecovery `AD3A35`, stress: armoredCasting `703B25`/attackStaminaCost `6B9709`/exhaustionPenalties `95FFFB`/massEffect `755649`; spells exhaustionController `062275`/baseAttackSpeed `03E556`/crossbowStagger `AE3597`; rescaling absorb `962799`/poison `962798`/ward `682FB5`); `playerExclusive` skill perks (alchemy `AD3A2F`, enchanting `AD3A30`, lockpicking `0BCC58`, pickpocket `AD3A31`, sneak `AD3A51`, sneakAttack `AD3A2E`, speech `AD3A2D`, tempering `8C8ED3`, **vampire `035E31`/werewolf `AD3A42`**); `stateTraits.vampire` keyword `0A82BB:Skyrim.esm` → perk `RFTI_Trait_Vampire AD3A44`; `racialTraits.werewolf` race `0CDD84` → `RFTI_Trait_Lycanthrope_Werewolf AD3A43`. All confirmed live (winners are MR/Resist-and-Regen patches — the `RFTI_*` assigned family).
  - **VAMPIRE/LYCANTHROPY decision: kept as a capstone REFERENCE, NOT split into its own skill** (decisive master call). Rationale: the integration work is *routing* to the existing race/npc/magic/script skills with the vampire-keyword/trait constraints — not a new procedural method needing its own evals. Documented stages/sun (12/sec cap 30)/holy-ground (4/sec)/feeding + the trait FormIDs + the per-domain recipes. (Promote later only if a full vampire-clan overhaul job appears.)
  - **Two handoff assumptions corrected live:** (1) **economy is record-driven** (vendor `CONT` → LVLI per NPC + base item values rounded to 5) — there is **no DynamicPricing JSON/SKSE config** in `Requiem - Vendor tweaks.esp`. (2) **lockpicking is an SKSE DLL** (`RequiemLP.dll`), **not record-patchable**. Both noted in the references + the checklist's "is-this-patchable" notes.

  **Verification:** scripts round-trip PASS (plain-effect-no-mechanic-script confirmed; the live `Nox_Conjuration_Teleport` attachment documented end-to-end). **Integration dry-run on `Lucien.esp` (18,969 records — a major follower + Mihail creature pack) PASS** (`requiem-patching/evals/results-2026-06-09.json`): enumerated by type (52 NPC_, ~12 creature RACE, dozens of MGEF/SPEL incl. a self-scripted teleport, 3 WEAP, creature skins, 6 CONT, large cosmetic layer), routed each to the right domain skill, named 4 gap mechanics. **Strong cross-check:** Lucien is literally in the live Authoria follower-bridge alias list (Inigo/Kaidan/Auri/Xelzaz/Mrissi/Sahara/Thogra/Yoana/Remiel/**Lucien**) — confirming the "follower → add to Authoria's registration quest" route is exactly what Authoria did. Creature→race-bridge and preserve-the-modded-teleport-script routes also matched established methods.

  **Flagged for Phase 9 (not fully resolvable / needs a final pass):** (a) the VMAD *write* path was documented from the read schema but **not exercised with a live houseCARL write** — Phase 9 should round-trip an actual VMAD attach via `bulk_apply`/`create_record` and confirm the exact modeled path (use `mutagen-reference` for `ScriptEntry`/`ScriptObjectProperty`). (b) `housecarl_compile_script` not run (CK compiler path). (c) disease payload MGEF winner varies (base Requiem vs SunHelm survival patch) — confirm live before cloning. (d) standing-stone activation registration via `REQ_Quest_Birthsign` not fully traced. (e) the per-stage vampire magnitudes are from the docs — confirm live vs `Requiem - VampireCollection.esp` when a job rebalances them. (f) **the follower-registration ESP is inactive** in this profile — Phase 9 / Aaron should decide whether to enable/extend it so new followers actually register. Plus the standing Phase-9 actions: masters/REQ_NULL back-fill into all 9 skills + the §8 audit + the no-REQ_NULL/masters-sorted gate + packaging.

  **Reuse for Phase 9:** both new skills follow the proven template (action-first description, freshness First step, real houseCARL call shapes, Judgment/Common-mistakes/Checklist/Notes, `references/` + `index.jsonl`, `evals/`). The masters & REQ_NULL discipline is folded into both (SKILL.md Checklist + Common-mistake + `housecarl-recipes.md` / `integration-checklist.md`). Item 19 build-script registry remains N/A (auto-discover).

- **Phase 9** (master session, 2026-06-09): **PROJECT COMPLETE — 9 skills, v1.0.0.**
  - **Back-fill:** the masters + `REQ_NULL`-strip gate added to the four pre-doc skills' Checklists (`requiem-weapon/armor/ammo/race-patching`) + a `REQ_NULL` Common-mistake in weapon (the only one with zero prior coverage). Decisive scope call: a verbose per-skill recipe duplication was *not* added — the actionable gate lives in each Checklist, the full mechanism in the shipped capstone `integration-checklist.md` + dev `docs/masters-and-null-stripping.md`. NPC/leveled/magic/script/capstone already carried it.
  - **§8 audit (all 9 skills):** every `name:` == folder ✓; descriptions 639–837 chars (≤1536 cap) ✓; bodies 131–304 lines (≤500 hard cap) ✓; all carry `index.jsonl` + `evals/eval_set.json`. Router (`requiem-patching`) `routing-table.md` dispatches to all 8 domain skills by correct name ✓. Items 16–18 remain on the §6.5 manual-prediction fallback (Windows blocks `run_loop`) — empirical re-validation + a peer prediction check are the standing QA follow-up.
  - **Packaging:** `README.md`, `CHANGELOG.md`, `authoria-requiem/skills/README.md` written; `plugin.json` → **1.0.0**; installable `marketplace.json → authoria-requiem/ → plugin.json + skills/` layout confirmed. Not git-initialized (left to Aaron per the local-first design).
  - **houseCARL 1.2.0 → 1.2.1 update** applied earlier this session (active-patch self-lock fixed; bug note removed from memory). houseCARL re-verified live (Iron Sword → `Requiem.esp`, 2842 active).
  - **Live verify — VMAD write round-trip PASS (Phase-8 item a):** `create_record` composed a `ScriptEntry` (`Name=Nox_Conjuration_Teleport`, `Flags=Local`) onto a new `MagicEffect`'s `VirtualMachineAdapter` (Version 5 / ObjectFormat 2 / Scripts[1]) — all-or-nothing accepted, read-back confirmed. Note: `Spell` has no `VirtualMachineAdapter` field (use the record type that does — MGEF/etc.). Nested `Properties` (ScriptObjectProperty) compose not separately exercised — the script-entry attach is proven; property clone-and-repoint is the documented remaining nuance. Test patch cleaned up.
  - **Remaining follow-ups (for a post-restart session / Aaron):** (b) `housecarl_compile_script` path not run (CK compiler), (c) disease payload MGEF winner varies (base Requiem vs SunHelm patch) — confirm live before cloning, (d) standing-stone activation quest `REQ_Quest_Birthsign` trace, (e) per-stage vampire magnitudes vs `Requiem - VampireCollection.esp`, (f) the follower-registration ESP (`Authoria - Master Patch - Requiem Follower Registration.esp`) is INACTIVE — Aaron to decide whether to enable it so new followers actually register. Plus the empirical eval re-validation above. None block use of the plugin.
  - **Housekeeping:** phase sessions left several inactive verification patches in MO2 (`houseCARL - Requiem weapon/armor/NPC/leveled-list/magic patching`, `Authoria_Races_*`, three `Dawnguard Arsenal` variants) — left in place (not the master's to delete); Aaron can prune/consolidate.
