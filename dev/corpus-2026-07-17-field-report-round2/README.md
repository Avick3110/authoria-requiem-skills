# Mining corpus — field-report round 2 (NPC / Magic Redone / consumables verify)

*Author: Heisen (+ Claude session) · 2026-07-17*

**Class:** ARCHIVE (frozen at capture). Raw empirical mining behind the 1.3.0 skill overhaul —
four parallel read-only houseCARL sweeps against the Authoria-dev order (RftI inactive,
hand-authored winners). The distilled doctrine lives in the skills' `references/`; these files are
the evidence trail.

- `mining-npc.md` — Requiem-lane NPC authority census; non-combatant treatment; the per-archetype
  stat model (DNAM base attributes / SkillValues / SkillOffsets / ACBS offsets / AutoCalc); the
  empty-kit finding; the field union Requiem edits on an NPC.
- `mining-mr-spells.md` — ManualCostCalc universality (827/833); cost + charge ladders with
  delivery multipliers; the multi-effect rider shapes per archetype; the 833-spell subclass
  census (tier band 0–5); the REQ_NULL resistance replacement map; tome value bands.
- `mining-mr-mgef-ench.md` — MGEF keyword/flags/values taxonomy (MinimumSkillLevel tier marker,
  Association doubling); MGEF condition patterns A–D; ENCH cost model + the gear-value coupling
  correction (enchant adds zero gold); staff/scroll conventions.
- `mining-consumables-verify.md` — claim-by-claim verification of the consumable references
  (29/30 confirmed; the one wrong FormID); AR/FaB type census; missed conventions (FLOR, thrown
  SCRL delivery, FaB race records, COBJ gate nuance).
